<div id='bg'>
  <div class='centered'>
    <h1>Wanderlease</h1>
    <% unless !current_user %>
      <p>Welcome, <%= current_user[:email]%></p>
    <% end %>
  </div>
  <h3>Listings</h3>
	<%= @listings.each do |listings| %>
		<tr>
			<td><%= listings.location %></td>
		</tr>
		<% end %>
</div>
